# API

# GET /message

+ Parameters
    + type: `foo` (enum[string]) - Filter by type
        + Members
            + `foo`
            + `bar`
            + `baz`

+ Response 200 (text/plain)

        Hello World!

